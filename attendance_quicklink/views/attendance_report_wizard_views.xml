<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_attendance_report_wizard_form" model="ir.ui.view">
        <field name="name">attendance.report.wizard.form</field>
        <field name="model">attendance.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Generar informe de fichajes">
                <group>
                    <field name="date"/>
                    <field name="margin_minutes"/>
                </group>
                <footer>
                    <button string="Generar Excel" name="action_generate_report" type="object" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_attendance_report_wizard" model="ir.actions.act_window">
        <field name="name">Informe de fichajes</field>
        <field name="res_model">attendance.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_attendance_report_wizard_form"/>
        <field name="target">new</field>
    </record>

    <record id="access_attendance_report_wizard" model="ir.model.access">
        <field name="name">access.attendance.report.wizard</field>
        <field name="model_id" search="[('model', '=', 'attendance.report.wizard')]"/>
        <field name="group_id" ref="hr_attendance.group_hr_attendance_officer"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <menuitem
        id="menu_attendance_report_wizard"
        name="Informe de fichajes"
        parent="hr_attendance.menu_hr_attendance_reporting"
        action="action_attendance_report_wizard"
        sequence="20"
        groups="hr_attendance.group_hr_attendance_officer"/>
</odoo>